# Stores the search query
InputSearch <- reactiveValues(search=NULL)

MainPlotValues <- reactiveValues(plotHeight=LOCUS_PLOT_HEIGHT)

PlotOneValues <- reactiveValues(plotHeight=LOCUS_PLOT_HEIGHT)

PlotTwoValues <- reactiveValues(plotHeight=LOCUS_PLOT_HEIGHT)

ProfilePlotValues <- reactiveValues(plotHeight=PROFILE_PLOT_HEIGHT)

SelectedCall <- reactiveValues(ID=NULL)

Links <- reactiveValues(IDs=c())

yAxisLimit <- reactiveValues(value=NULL)

